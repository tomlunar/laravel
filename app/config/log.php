<?php

return array(

	/*
	|--------------------------------------------------------------------------
	| Log file
	|--------------------------------------------------------------------------
	|
	| By default we will build a basic log file setup which creates a single
	| file for logs. The artisan command tail uses this file both locally
	| and remotely, though it can be overriden using the --path option.
	|
	*/

	'file' => storage_path().'/logs/laravel.log',

);
